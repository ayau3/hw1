function move(spaces, direction) {
  var count = 0
  while (count < spaces) {
    direction
    count = count + 1
  }
}
move(2, down)
var color = getColor
move(3, down)
move(2, right)
setColor(color)
move(2, right)
up
